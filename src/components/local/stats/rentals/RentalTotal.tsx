import { Box } from "@chakra-ui/react"
import BoxWrapper from "../../../layout/local/BoxWrapper"

const RentalTotal = ({ data }) => {
  return (
    <Box w={["100%", "50%"]}>
      <BoxWrapper>asdf</BoxWrapper>
    </Box>
  )
}

export default RentalTotal
